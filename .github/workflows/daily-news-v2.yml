name: DISABLED - Daily News Update (Optimized)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow (v2) is disabled. Use daily-news.yml instead."
